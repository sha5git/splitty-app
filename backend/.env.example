# Copy to your shell profile, IDE run config, or export before starting the backend.
# DB_PASSWORD is required — there is no default in application.properties.

DB_URL=jdbc:postgresql://localhost:5432/expensesplit
DB_USERNAME=postgres
DB_PASSWORD=your-password-here
